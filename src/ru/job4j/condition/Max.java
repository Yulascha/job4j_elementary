package ru.job4j.condition;

public class Max {
    public static int max(int left, int right) {
        return left >= right ? left : right;
    }

    public static int max(int left, int right, int centr) {
        return int max, int max(int left, int right);
    }

    public static void main(String[] args) {
        int left = 1;
        int right = 2;
        int centr = 3;
        int result = Max.max(left, right, centr);
        System.out.println(result);
    }
}